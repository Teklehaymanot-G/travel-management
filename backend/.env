DATABASE_URL="mysql://root@localhost/travel_db?schema=public"
JWT_SECRET="your_strong_secret_here"
PORT=5000
NODE_ENV=development

# JWT
JWT_EXPIRES_IN="7d"

# Server
PORT=5000
NODE_ENV=development
# Image host (optional override; set to LAN IP for mobile devices)
# IMAGE_HOST="http://192.168.8.111:5000"
# Explicit unified base URL for images (overrides request host so web won't use localhost)
IMAGE_BASE_URL="http://192.168.8.111:5000"

# File Upload
UPLOAD_PATH="./uploads"
MAX_FILE_SIZE=5242880

# Email (for notifications)
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT=587
EMAIL_USER="your-email@gmail.com"
EMAIL_PASS="your-app-password"